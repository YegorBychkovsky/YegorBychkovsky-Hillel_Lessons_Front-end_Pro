import React from "react"
import styles from "../Left-section/Section.module.css"

export const LeftSection = () => {
    return (
        <nav className={styles.section} >
            LEFT SECTION
        </nav>
    )
}