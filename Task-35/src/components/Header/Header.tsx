import React from "react"
import style from "../Header/Header.module.css"


export const Header = function headerCreate() {
    return (
        <header className={style.header}>
            HEADER
        </header>
    )
}